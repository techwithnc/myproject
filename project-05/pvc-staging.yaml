apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: staging
  name: pvc
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: scn01
  accessModes:
    - ReadWriteOnce